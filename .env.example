# ─── Required ────────────────────────────────────────────
# Your Gemini API key (users can also enter it in the UI)
GEMINI_API_KEY=AIzaSyANU5jhjDJVzk-PNo0K27-hz9U5K_kGBrY

# ─── Optional ────────────────────────────────────────────
# Gemini model to use (default: gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# Server port (default: 3000)
# PORT=3000

# Bind address (default: 0.0.0.0)
# HOST=0.0.0.0

# Rate limit: max analysis requests per IP per minute (default: 10)
# RATE_LIMIT=10

# Set to "production" for static-asset caching
# NODE_ENV=production
